meta {
  name: Refresh token
  type: http
  seq: 5
}

post {
  url: {{URL}}/auth/tokens/refresh
  body: none
  auth: inherit
}

script:post-response {
  var data = res.getBody();
  bru.setEnvVar("Token", data.token);
}

settings {
  encodeUrl: true
  timeout: 0
}
